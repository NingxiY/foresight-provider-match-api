server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback: let React Router handle client-side routes on
        # refresh/direct access instead of Nginx 404ing on unknown paths.
        try_files $uri $uri/ /index.html;
    }
}
