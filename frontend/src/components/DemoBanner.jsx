export default function DemoBanner() {
  return (
    <div className="demo-banner">
      This is a class demo running on seeded mock data — no real patients or providers are involved.
    </div>
  );
}
